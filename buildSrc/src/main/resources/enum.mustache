// THIS FILE IS AUTO-GENERATED. DO NOT EDIT!

package {{packageName}};

import com.viaversion.viaversion.libs.fastutil.ints.Int2ObjectMap;
import com.viaversion.viaversion.libs.fastutil.ints.Int2ObjectOpenHashMap;

public enum {{enumName}} {

    {{#each values}}
    {{#if comments}}
    /**
    {{#each comments}}
     * {{.}}{{#if iterHasNext}}<br>{{/if}}
    {{/each}}
     */
    {{/if}}
    {{name}}({{value}}){{#if iterHasNext}},{{/if}}{{#unless iterHasNext}};{{/unless}}
    {{/each}}

    private static final Int2ObjectMap<{{enumName}}> BY_VALUE = new Int2ObjectOpenHashMap<>();

    static {
        for ({{enumName}} value : values()) {
            if (!BY_VALUE.containsKey(value.value)) BY_VALUE.put(value.value, value);
        }
    }

    public static {{enumName}} getByValue(final int value) {
        return BY_VALUE.get(value);
    }

    public static {{enumName}} getByValue(final int value, final {{enumName}} fallback) {
        return BY_VALUE.getOrDefault(value, fallback);
    }

    private final int value;

    {{enumName}}(final int value) {
        this.value = value;
    }

    public int getValue() {
        return this.value;
    }

}
